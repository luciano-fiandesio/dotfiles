# First section: remote IMAP account
IMAPAccount fastmail
Host imap.fastmail.com
Port 993
User lfi@fastmail.fm
PassCmd "gpg -q --for-your-eyes-only --no-tty --exit-on-status-write-error --pinentry-mode loopback --passphrase-file /home/luciano/pass.txt -d /home/luciano/fastmail.fm.gpg"
SSLType IMAPS
SSLVersions TLSv1.2

IMAPStore fastmail-remote
Account fastmail

# This section describes the local storage
MaildirStore fastmail-local
Path ~/.local/share/email/
Inbox ~/.local/share/email/INBOX
# The SubFolders option allows to represent all
# IMAP subfolders as local subfolders
SubFolders Verbatim

# This section a "channel", a connection between remote and local
Channel fastmail
Far :fastmail-remote:
Near :fastmail-local:
Patterns  "INBOX" "Archive" "Sent Items" "Junk Mail" "Drafts" "Trash" "berlin" "paper trail" "the feed" "Set Aside" "work" "travel" "services" "family" "job-proposal" "Reply Later"
# Do not delete message
Expunge None
# Makes sure the date of the arrival stays the same when you move messages around. 
CopyArrivalDate yes
Sync All
# Automatically create missing mailboxes, both locally and on the server
Create Both 
SyncState *
